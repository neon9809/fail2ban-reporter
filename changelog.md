# 主要功能更新：

## AbuseIPDB API密钥配置：
您现在可以通过设置ABUSEIPDB_API_KEY环境变量来配置您的AbuseIPDB API密钥。

### IP信誉查询：
当检测到ABUSEIPDB_API_KEY配置后，程序会在生成报告前，自动查询所有被封禁（Ban）IP的信誉详情。

### 智能额度管理：
- 在进行IP查询前，程序会首先检查您的AbuseIPDB账户剩余的API查询额度。
- 如果额度充足，则执行查询并正常在报告中展示结果。
- 如果额度不足，程序会跳过查询，在发送完常规报告后，额外发送一封邮件，通知您API额度不足的情况。

### 报告模板更新：
report-template.html已更新，当存在AbuseIPDB查询结果时，会以表格形式详细展示每个IP的信誉信息，包括滥用置信度、国家、ISP、使用类型等所有返回的字段。